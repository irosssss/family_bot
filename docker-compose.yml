# Local V3 preview only. Existing database volumes are not removed or migrated.
services:
  app:
    build: .
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
    ports:
      - "127.0.0.1:3000:3000"
