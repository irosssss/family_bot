# Разбор файлов относительно V3 — 22.09.2026

Результат после объединения: единственный интерфейс — V3.

| Категория | Пути | Результат |
|---|---|---|
| Используется сейчас | src/main.tsx, src/v3/*Panel.tsx, V3App.tsx, domain.ts, session.ts, components | Единый вход dev/build; зависимости от demo нет |
| Используется сейчас | content-source/v3, public/assets/game/v3-ui, public/assets/game/wardrobe-pilot/v3 | 79 определений, 36 файлов графики; ссылки проверяются CLI |
| Подготовлено для следующих этапов | src/target, src/v3-server, src/v3-shared, их миграции и тесты | Сохранены; назначение в docs/maintenance/PREPARED_MODULES.md |
| Заменено новой реализацией | прежние App/demo/live, legacy API/бот, пиксельные каталоги | Удалены из рабочего дерева; доступны в истории Git |
| Повторно создаваемые сборки | dist, dist-wardrobe-pilot, work/*-build | Пересобираются командами; не включаются в Git |

Перед очисткой проверены статические зависимости TypeScript, отдельные динамические
пути каталогов и точки запуска. Старые API не обслуживаются новым preview-сервером;
существующие базы, Docker volumes и браузерные сохранения не изменялись.
Тесты удалённых реализаций удалены вместе с ними; тесты текущей V3 и подготовленных
серверных модулей сохранены.

Реестр: source-files.csv и summary.json. Полный локальный вывод:
work/repository-audit/files.csv. Повторить: node scripts/maintenance/inventory.mjs.
review_required означает, что назначение оставшегося материала ещё нужно уточнить,
а не разрешение удалить его. Исторический build-copies.json описывает состояние
до очистки и не является текущим составом сборки.
