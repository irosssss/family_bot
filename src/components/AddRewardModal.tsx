import React, { useState } from 'react';
import { X, Gift } from 'lucide-react'; interface AddRewardModalProps { isOpen: boolean; onClose: () => void; onAddReward: (reward: { title: string; cost: number; reward_type: 'personal' | 'joint' }) => void;
} export const AddRewardModal: React.FC<AddRewardModalProps> = ({ isOpen, onClose, onAddReward }) => { const [title, setTitle] = useState(''); const [cost, setCost] = useState(25); const [rewardType, setRewardType] = useState<'personal' | 'joint'>('personal'); if (!isOpen) return null; const handleSubmit = (e: React.FormEvent) => { e.preventDefault(); if (!title.trim()) return; onAddReward({ title: title.trim(), cost: Number(cost), reward_type: rewardType, }); setTitle(''); onClose(); }; return ( <div className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black/70 backdrop-blur-sm"> <div className="bg-[#171c28] border border-white/10 rounded-2xl w-full max-w-md p-6 shadow-2xl space-y-4"> <div className="flex items-center justify-between"> <div className="flex items-center gap-2"> <Gift className="w-5 h-5 text-amber-400" /> <h3 className="text-base font-bold text-white">Добавить награду</h3> </div> <button onClick={onClose} className="p-1 rounded-lg text-slate-400 hover:text-white"> <X className="w-5 h-5" /> </button> </div> <form onSubmit={handleSubmit} className="space-y-4"> <div> <label className="text-xs font-semibold text-slate-300 block mb-1"> Название награды </label> <input type="text" required placeholder="Например: Кофе в постель, 2 часа в игре..." value={title} onChange={(e) => setTitle(e.target.value)} className="w-full bg-white/5 border border-white/10 rounded-xl px-3.5 py-2 text-xs text-white placeholder-slate-500 focus:border-amber-500 outline-none" /> </div> <div className="grid grid-cols-2 gap-3"> <div> <label className="text-xs font-semibold text-slate-300 block mb-1"> Стоимость (Золото золото) </label> <input type="number" min={5} max={500} required value={cost} onChange={(e) => setCost(Number(e.target.value))} className="w-full bg-white/5 border border-white/10 rounded-xl px-3.5 py-2 text-xs text-white focus:border-amber-500 outline-none" /> </div> <div> <label className="text-xs font-semibold text-slate-300 block mb-1"> Тип награды </label> <select value={rewardType} onChange={(e) => setRewardType(e.target.value as any)} className="w-full bg-white/5 border border-white/10 rounded-xl px-3 py-2 text-xs text-white focus:border-amber-500 outline-none" > <option value="personal" className="bg-slate-900">Личная </option> <option value="joint" className="bg-slate-900">Совместная </option> </select> </div> </div> <div className="pt-2 flex justify-end gap-2"> <button type="button" onClick={onClose} className="px-4 py-2 rounded-xl text-xs text-slate-400 hover:text-white bg-white/5" > Отмена </button> <button type="submit" className="px-5 py-2 rounded-xl text-xs font-bold text-slate-950 bg-amber-400 hover:bg-amber-300 shadow-md shadow-amber-500/20" > Добавить в магазин </button> </div> </form> </div> </div> );
};
