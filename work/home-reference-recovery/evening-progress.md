# Вечерний дом — фактический статус

2026-09-05. Эталон выбран пользователем; новый дом в приложении ещё не собран и не принят.

**Последующий отзыв пользователя: оба вечерних кандидата отвергнуты — «выглядит ужасно, мало пространства».** Слишком крупная мебель и тесное вертикальное кадрирование не подходят. Прежняя рекомендация укоротить кадр не актуальна как направление. Следующий шаг: более общий план комнаты, меньшая мебель, близкая семья2+2сразу в кадре для проверки вместимости. Не превращать это в пустой зал. До пространственной приёмки не обрабатывать отвергнутый фон как финальный набор слоёв.

## Что сделано

- Сохранён один исходник: первый верхний левый экран `work/references/visual-v2/05-main-flow-directions.png`. Дневной дом исключён из художественных решений.
- Встроенный image_gen подготовил два непрозрачных кандидата чистого интерьера. Это реконструкция по низкоразмерному исходнику, не извлечение отсутствовавших за людьми пикселей.
- Независимый визуальный агент проверил первую итерацию. Вторая уменьшила лишний верх, но не полностью сократила нижний ковёр: её нельзя объявлять точным совпадением. Пока не фиксировать окончательные якоря по ней.
- Инженер подготовил `src/demo/homeSceneProjection.ts`: одна геометрическая проекция всех authored-слоёв без независимого масштаба детей/родителей. Не подключена к HomeScreen, не меняет текущие комнаты или внешний вид.

## Художественные файлы вне runtime

| Имя | Размер | Путь | Использование | SHA12 |
| --- | --- | --- | --- | --- |
| Подложка-кандидат v1 | 887×1774 RGB PNG | `work/home-reference-recovery/evening-background-candidate-v1.png` | Независимая визуальная сверка; не принята | `72355498d33c` |
| Подложка-кандидат v2 | 874×1800 RGB PNG | `work/home-reference-recovery/evening-background-candidate-v2.png` | Рабочая коррекция кадрирования; не принята | `c6b903eb917a` |

Оригиналы generated_images сохранены; workspace-копии выше неизменны. Промпты и инварианты: `evening-prompts.md`. Режим встроенный, не CLI/API. Никакие растровые пиксели не редактировались скриптами в этом этапе.

## Проверки

- `npm run lint`:0 ошибок.
- `npm test`:168passed,9skipped; пропущен opt-in PostgreSQL набор.
- `homeSceneProjection.test.ts`:21проверка единой проекции, разных размеров и невалидной геометрии входит в168.
- Первый полный запуск внутри sandbox не смог открыть временные127.0.0.1порты и дал3failed suites; после разрешённого escalation тот же npm test успешен. Код тестов/таймауты ради обхода не менялись.
- `git diff --check`:чисто.
- Новый браузерный визуальный проход пока не выполнялся: новые фон и проекция не подключены в UI. Старая визуальная приёмка дома по-прежнему отклонена пользователем.

## Следующий шаг и граница разрешений

Для SpriteCook подготовлены и прочитаны skills workflow-essentials, upload-assets и generate-sprites. MCP доступен. Повторный get_credit_balance:80 (40subscription+40topup), одновременная задача1. Файлы не отправлялись, платные операции не запускались.

Ранее auto-review остановил отправку локального PNG в SpriteCook без отдельного подтверждения файла и получателя. Не обходить этот запрет через inline import, shell, агента или другой сервис. Запрашивается прямое разрешение на загрузку **`work/references/visual-v2/05-main-flow-directions.png` в SpriteCook** и обработку отдельных частей этого дома за имеющиеся кредиты, без покупки/пополнения. Подложки-кандидаты и остальная папка этим узким запросом не передаются.

После разрешения: загрузить один референс через штатный upload bridge; сохранить asset_id/SHA без upload-secret; сначала подготовить и проверить один прозрачный компонент. Проверять актуальную стоимость до генерации, реальную alpha и границы. Далее тот же источник — для поз семьи, мебели и масок; не генерировать unrelated-варианты. Только после сравнения слоёв и их сборки — HomeScreen,375/390, пользовательская приёмка, остальные страницы.
